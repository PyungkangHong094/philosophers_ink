# QA: M4 게이트 (챕터 1–2 레벨 33 + 셸 1차) — 2026-07-18

검증자: qa-m4 · 대상: assets/levels/level_001~033.json (levels-ch1/ch2a/ch2b) + lib/ui·lib/meta·lib/main.dart (shell-m4) ·
기준: docs/PHILOSOPHERS_INK_DESIGN.md(7장 진행·8.4 셸)·LEVELS.md(§5 작업 레벨·§6 설계표·§8 검증)·ink-qa-protocol.
환경: Flutter 3.44.6 / Dart 3.12.2, `env -u PUB_CACHE -u TMP -u TEMP` 접두 (스트레이 C:\pub-cache 미발생).

## 요약: PASS 12 / FAIL 0 / 권고 5 / 기준미정의 0 → **GO**

P0·P1 결함 없음. 팀 보고 수치(analyze 클린·테스트 180·레벨 33/33)를 전부 실측 재현. 레벨 콘텐츠는
해금 위반 0건(챕터 3+ 물질·기믹 유출 없음), 셸 토큰 준수(하드코딩 hex 0), 실제 AOT 앱 빌드 성공.

---

## 항목별 판정

### 1. 실측 재현 — PASS
- `flutter analyze`(lib+test): **No issues found!** (3.6s).
- `flutter test`: **All tests passed! (+180)** — 팀 보고 180개 일치. loader_test는 스모크 루프
  (assets/levels 전수 loadLevelFromJson 통과)로 전환 확인, 이전 M4 셸 리포트의 level_001 chalk 100/250
  드리프트 실패는 해소됨(회귀 재검 통과).

### 2. 레벨 콘텐츠 감사 (33개 전수, 스크립트 파싱) — PASS
- **로더 전수 PASS**: loader_test 스모크가 33개 전부 loadLevelFromJson→validateLevel 통과.
- **해금 위반 0건**: 전 33레벨에서 관측된 물질 = {PRIMA, WALL, WATER, ICE}뿐. 챕터 3+ 물질
  (STEAM/ASH/LAVA/STONE) 유출 0. 기믹 = {gravity_flip, portal, temp_zone, variance_gate}뿐, ash_emitter 0.
  - 챕터 1(1–11): chalk 전용(heat=frost=0), 기믹 gravity_flip만, 상태/순수/유한 방출 미사용 — 전수 준수.
  - 챕터 2(12–33): heat=0 전수, 기체 플라스크 0(solid/liquid만), 순수 0, 유한 방출 0 — 전수 준수.
- **§6 설계표 정합**: 이름·D값 33개 전부 표와 일치. 교육 레벨 teaches도 새 요소와 일치
  (L1 석필·PRIMA / L8 gravity_flip / L12 water / L15 temp_zone / L17 frost_rune / L21 state_flask /
  L23 portal / L25 variance_gate). ch2b의 **L026 상이 1건**(상태 플라스크 조건 제거, material:PRIMA만)은
  노브 2개 동시 상승 회피 목적의 보고된 의도 변경 — 핵심 아이디어(역게이트 왕복) 유지, **타당**.
- **D 곡선·dip**: 챕터1 = 1,1,2,2,3,3,4,**2**,4,5,5 (L8 gravity 교육 dip). 챕터2 = 2,3,3,3,3,3,4,4,5,**4**,6 /
  3(dip),5,**4**(dip),5,5,6,6,6,7,7,7. 신규 요소 레벨(21 상태·23 포탈·25 변성)이 전부 국소 최소(dip)에 배치.
- **작업 레벨(11/22/33)**: hint_stroke 전부 null ✓. 골드 링은 `isOperatioLevel = id%11==0`로 파생
  (chapters.dart:96) — 이름 문자열 비의존. D 스파이크 5→6→7 단조 증가(§5 준수).
- **잉크 여유율 표본(챕터2, optimal 존재분)**: D밴드 공식 부합. L12 D2=250%, L17 D3(frost)=200%,
  L20 D5=170%, L24 D5=170%, L28 D6=170%, L31 D7≈147%, L33 D7≈149%, 교육 dip(23=267%/25=300%)은 관대 처리.

### 3. 경계면: 셸 카탈로그 ↔ 매니페스트 ↔ 로더 — PASS
- `LevelCatalog.discover`가 AssetManifest에서 `assets/levels/*.json`을 스캔→loadLevelFromJson으로 파싱,
  성공분만 노출·실패는 loadErrors 기록(조용한 스킵 금지). pubspec `assets/levels/` 디렉토리 등록으로
  33개 전부 매니페스트에 포함 → 전부 열거·로드. id 오름차순 보장.
- **별점·해금 로직 (GDD 7.1)**: 챕터 경계 11의 배수, 하위 챕터 존재 레벨 전부 클리어 시 상위 챕터 해금,
  챕터 내부 선형 해금(직전 레벨 클리어). 별점 최고치 유지·변화 시에만 영속. GDD 7.1 챕터 경계 모델과 일치.

### 4. 셸 디자인 토큰 준수 — PASS
- `grep Color(0x lib/ui lib/meta` (tokens.dart 제외) = **0건**.
- InkColor 팔레트가 GDD 8.4.2와 정확 일치: black0 #050505 · black1 #0A0A09 · hairline #29271F ·
  gold #C9A227 · goldHi #E8C95A · goldDeep #8F7118 · parchment #F2EDDF.
- 구 디버그 UI: main.dart는 InkApp 부팅으로 교체(디버그 셸 제거). level_player는 에디터의 테스트 플레이로
  존속하고 에디터는 settings_screen에서 `kDebugMode` 가드 히든 진입 — 처리 적절.

### 5. 메타 영속화 — PASS (권고 1)
- progress_test "직렬화 왕복": GameProgress.toJson→fromJson이 별점·클리어·집계 보존(데이터 손실 지점 커버).
  garbage 입력 폴백도 검증. ProgressStore는 JSON↔prefs 얇은 브리지(로드 실패→빈 진행 폴백).
- 권고: prefs 목(setMockInitialValues) 경유 save→reopen→restore 통합 테스트는 부재(아래 권고 3).

### 6. 빌드 — PASS (주의: macos 대상 부재)
- 게이트 지정 `flutter build macos --debug`는 exit 0이나 **실제 빌드 아님** — 프로젝트에 macOS 대상
  미구성("No macOS desktop project configured"). 구성 플랫폼 = android/ios/windows.
- 실 컴파일 증거로 **`flutter build ios --debug --no-codesign` 직접 실행 → 성공**: Xcode build done 141.8s,
  `build/ios/iphoneos/Runner.app`(106M) 산출. 셸 포함 전체 앱 AOT 빌드·링크 정상.
- (출시 대상 Windows는 이 darwin 호스트에서 빌드 불가 — 환경 한계, 코드 결함 아님.)

### 7. 결정성·성능 회귀 — PASS
- determinism_test·gimmicks_determinism_test(재시작 3회 동일 해시) 180개에 포함·통과 — 셸 도입이 sim 미영향.
- 성능 벤치 인테스트 실측: **기믹 활성 틱 평균 0.982ms** (활성 셀 4550, 예산 3ms) — 회귀 없음.

---

## 권고 (문서 근거 약함·비차단)
1. [P2] 작업 레벨 JSON 이름이 `하소`/`용해`/`분리`로 §6 표의 `◈` 접두 없음. 골드 링은 id%11로 파생하므로
   **기능 영향 0**(오히려 표시명에 글리프 미포함이 자연스러움). 표시명에 ◈ 노출 여부만 정책 결정. 소유: levels.
2. [P2] L16(빙결 지대) teaches=[]인데 §6은 온도 존(냉)을 새 요소로 표기. ch2a 리포트가 "동일 기믹 type의
   냉 변형이라 teaches 대신 tags:[cool]"로 문서화 — 의도적. 신규 요소 온보딩 문구가 붙는지만 UX 확인 권장. 소유: levels.
3. [P2] ProgressStore prefs 왕복 통합 테스트 부재(직렬화 로직 자체는 테스트됨). 회귀 안전망으로 추가 권장. 소유: shell-m4.
4. [P2] `lib/gameplay/debug_hud.dart`가 프로덕션 경로에서 미참조(doc 코멘트 + 자기 테스트만) — 사장 코드 가능성.
   정리 또는 존치 사유 명기. 소유: gameplay.
5. [P2] 클리어 화면 골드 3요소(별·게이지 필·CTA)는 GDD 8.4.4가 명시 나열 → 스펙 준수. 다만 일반 원칙(1~2개)
   초과라 M5 폴리시에서 게이지 필 무채화 검토(shell 리포트 5절과 동일 의견). 소유: shell-m4.

## 백로그 (알려진 한계 — FAIL 아님)
- optimal_ink 전량 설계 추정치(실플레이 미실측). star_thresholds는 null로 공식 파생 동작. 플레이테스트 후 승격 필요.
- 손 플레이테스트(클리어 가능성 실증)는 인간 태스크로 이연. 특히 L9/L29 중력 반전 타이밍, L31 chalk 0,
  L17/19 서리 안식각 의존 레벨은 sim 실측 튜닝 대상.
- 별점 누적 챕터 보너스 레벨(GDD 7.1 line 187) 미구현 — M5+ (셸 1차 범위 밖, 기본 해금은 구현됨).

## 회귀 확인 (이전 리포트 대비)
- qa_m3_gate(GO) 대비: 결정성·기믹·성능 테스트 전부 유지 통과(160→180, 신규 20 = meta/catalog/shell 스모크).
- M4 셸 리포트가 보고한 유일 실패(loader_test level_001 chalk 드리프트)는 스모크 루프 전환으로 해소, 회귀 0.
