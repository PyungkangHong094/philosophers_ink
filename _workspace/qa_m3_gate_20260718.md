# QA: M3 게이트 (기믹 5종) — 2026-07-18

검증자: qa-m3 · 대상: sim-m3(lib/sim/gimmicks.dart·rules.dart, lib/core/game_state.dart) +
gameplay-m3(lib/level/*, lib/gameplay/level_session.dart) · 기준: GDD 6장·10장, 계약 문서
`_workspace/m3_sim_api_20260718.md`·`_workspace/m3_gameplay_report_20260718.md`.

## 요약: PASS 7 / FAIL 0 / 권고 2 / 기준미정의 0 → **GO**

P0 결함 없음. 보고 수치(테스트 160·analyze 클린·틱 ~0.97ms) 실측 재현 확인.

---

## 항목별 판정

### 1. 실측 재현 — PASS
- `dart analyze`: **No issues found!** (env -u PUB_CACHE -u TMP -u TEMP 접두, 스트레이 C:\pub-cache 미발생).
- `flutter test`: **All tests passed! (+160)**. 팀 보고 160개와 일치.
- 환경: Flutter 3.44.6 / Dart 3.12.2. 빌드 정상(스킵·무음 실패 없음).

### 2. 경계면 교차 검증 (JSON→로더→빌더→GameState→틱) — PASS
- **틱 순서 계약 일치**: `GameState.tick` = `_emit()` → `rules.step`. `rules.step`(rules.dart:52) =
  `_applyLineTransitions`(룬) → `_applyTemperatureZones` → `_applyGates` → `_applyMovement` →
  `_applyPortals`. 계약 §7 "방출→상전이(룬→온도존)→게이트→이동→포탈"과 코드 동시 대조 일치.
- **shape 일치**: 로더가 파싱하는 `GimmickSpec{type,params}` → `buildGimmicks` →
  `GimmickBundle{gates,portals,zones,hasGravityFlip}` → `GameState(gates:,portals:,temperatureZones:)`
  → `rules.step(gates:,portals:,zones:)`. 좌표계 `y*gridWidth+x` row-major가 sim 팩토리·빌더
  (`_rectCells`)·계약 3자 동일.
- **물질 ID 단일 소스**: 게이트/포탈이 `Material.<name>.index`, 검증기는 `materialFromName`
  (kMaterialTable 역인덱스) 사용 — 중복 enum 정의 없음.
- **직렬화 왕복 무손실**: `gimmick_loader_test.dart` "serialize→load가 기믹 params 보존" 통과
  (제네릭 params 맵 그대로 왕복).

### 3. 결정성 — PASS
- `gimmicks_determinism_test.dart`: 기믹 5종 전부 활성 + 중력 토글(틱60 on/180 off) → 300틱 후
  해시 3회 동일. reset 후 재현도 동일(중력 미복원 여부까지 assert).
- `gimmick_wiring_test.dart`: LevelSession 레벨에서 중력 토글 포함 재시작 3회 동일 해시,
  reset 후 재생 동일. gravityLog가 (틱,값) 기록 → 외부 재생 재현 확인(자동 재생 아님 = 계약대로
  gameplay 소유).
- 온도 존만 RNG 소비, 전이 불가 물질은 RNG 미소비 — `gimmicks_test.dart`에서 probe RNG로 검증.

### 4. 기믹 5종 거동 (GDD 6장 대비) — PASS
- 변성 게이트: from 지정 시 그 물질만, null 시 이동 물질 전부 변환·정적/EMPTY 보존. 낙하 흐름
  통과 변환 확인.
- 중력 반전: 정상=입자↓·기체↑, 반전=입자↑·기체↓ 미러링. reset이 기본(아래) 복원.
- 포탈: 입구→출구 텔레포트, 출구 막힘 시 입구 대기, 한 틱 1회(연쇄 방지) — move-stamp 근거.
- 온도 존: 룬 없이 존 셀 자신 가열/냉각(ICE→WATER→STEAM / 역), 존 밖 불변, probability=0 전이 없음.
- 재/순수: 방출구 ashRatio로 ASH 방출(결정성 RNG), pure 플라스크에 ASH 혼입 시 contaminated+재제거
  +isFailed(flask.dart:99). sim 방출 ↔ gameplay 순수 판정 조합 정합.

### 5. 성능 — PASS (예산 3ms)
- 인테스트(JIT VM) 실측: **0.990ms** (활성 셀 4550).
- 추가 AOT 컴파일 실측(프로파일 등가, 자체 벤치): **0.87~0.94ms**, 활성 셀 **14,807**(테스트보다
  고밀도), iters=2000. 예산 3ms 대비 3배 이상 여유.

### 6. 매직 넘버 감사 — PASS
- gimmicks.dart·gimmick_builder.dart 숫자 리터럴 = 루프 인덱스(0,1)·방어적 fallback(0,0,1,1)뿐 —
  밸런스 값 하드코딩 0건. 온도 존 기본 강도는 `SimConstants.pHeat/pCold` 참조(코드 상수 무),
  존별 강도는 레벨 JSON `probability`(레벨 데이터).

### 7. 검증기 부정 경로 — PASS
- gate `to` 누락·정적 물질·존 그리드 밖, 포탈 입출구 셀 수 불일치·rect 누락, 온도 존 kind 오류·
  probability 범위 밖, 알 수 없는 type, 해금 위반(챕터1 포탈) — 전부 명시적 LevelException.
  조용한 스킵 없음(`gimmick_loader_test.dart` 부정 경로 9종 통과).

---

## 권고 (문서 근거 없는 개선 제안, 차단 아님)
- [P2] `validator.dart` `_gimmickUnlockChapter` 맵 키가 원시 문자열('gravity_flip' 등)로,
  `GimmickType` 상수(level_model.dart)를 재사용하지 않는다. type 문자열 변경 시 컴파일러가
  불일치를 잡지 못함 — 매직 스트링 단일 소스 원칙상 상수 참조 권장. 소유: gameplay-m3.
- [P2] 로더에서 `params` 필드 자체 누락 시 조용히 `const {}`로 폴백(오류 없음). type별 필수 파라미터
  검증이 뒤에서 잡으므로 실무 영향은 없으나, 일관성상 명시 경고 고려. 소유: gameplay-m3.

## 회귀 확인 (이전 리포트 대비)
- qa_m2_gate/qa_m2_gameplay(2026-07-16) 모두 FAIL 0. M2 테스트 115개가 현재 160개에 포함,
  전부 통과 — 이동 규칙 중력 부호 리팩터(_gravitySign 기본 +1)로 인한 회귀 없음.
- 출고 레벨 level_001(챕터1)·level_021(챕터2) 모두 `gimmicks: []` — M3 기믹 콘텐츠 미투입,
  해금 위반 없음(콘텐츠는 M4 level-designer 소유).

## 사후 회귀 (P2 권고 반영 후, 2026-07-18)
- gameplay-m3가 P2-1(validator 해금 맵 → GimmickType 상수 참조)·P2-2(loader params 비객체 시
  명시적 _err) 반영. 재검: `dart analyze` 클린, `flutter test` **161 전부 통과**(신규 1). 회귀 0.
  게이트 판정(GO) 유지.
