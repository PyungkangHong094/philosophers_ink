# QA: M2 게이트 (통합 최종) — 2026-07-16

검증자: game-qa / 대상: M2 통합 (M2-A 플라스크·별점 + M2-B sim 방출구/착수 + M2-C 로더·에디터 +
LevelSession/LevelPlayer 통합). 착수 조건: #9·#10·#11 완료 + gameplay-engineer 통합 통지 + pubspec clean.
판정 기준: GDD 5·10.5·14장, LEVELS 4장, ink-qa-protocol. M1 성능 baseline(A/B/C).

## 요약: PASS 9 / FAIL 0 / 권고 2 / 보류 1

**M2 게이트: GO.** 코어 루프 완주 확인, 통합 경계면·결정성·성능 전부 통과.

| # | 항목 | 결과 |
|---|------|------|
| 1 | dart analyze (전체) 이슈 0 | PASS |
| 2 | flutter test 전체 115/115 | PASS |
| 3 | 경계면 sim↔gameplay↔level (방출구·착수·예산) | PASS |
| 4 | 통합 결정성 (이벤트 로그 포함, reset 3회) | PASS |
| 5 | **코어 루프 완주** (시작→플레이→클리어→재시작→재클리어) | PASS |
| 6 | 성능 회귀 (A/B/C + 통합 세션 틱, 예산 3ms) | PASS |
| 7 | 별점 통합 (totalUsed↔공식) | PASS |
| 8 | 레벨 JSON 왕복 무손실 (width 포함) | PASS |
| 9 | 매직 넘버 (밸런스 = 상수/JSON) | PASS |

## 상세

### [1] dart analyze — PASS
전체 `dart analyze` → "No issues found!" (sim #10 통합 완료로 전체 컴파일 정상).

### [2] flutter test 115/115 — PASS
플러그인 워크어라운드 후 전체 통과 (sim 8파일 + gameplay 6 + level 4 + smoke/widget).
신규 통합 테스트(level_session·emitter·phase·editor_screen) 포함, 실패 0. gameplay-engineer 보고
115와 일치. **P0 게이트 충족**: pubspec.yaml/lock/generated_plugins.cmake 3파일 전부 HEAD 복구
(git status clean), 임시 스크립트 삭제.

### [3] 경계면 sim↔gameplay↔level — PASS (양쪽 코드 동시 대조)
- **방출구 매핑**: level JSON → EmitterSpec(width 추가) → LevelSession._mapEmitters → sim EmitterConfig →
  GameState._emit. width 밴드, intervalTicks(rate.round, <1 방어적 1), 유한 total(remaining 감소·exhausted
  정지), ashRatio 결정성 RNG(**ashRatio>0일 때만 rng 소비** — 결정성 규율 유지). reset이 emitter 잔량 복원.
- **착수 소비**: FlaskSystem.update가 grid.set(empty)로 소비, consumeCell(GameState)도 RNG 미사용.
  상태 플라스크 비매칭 상 셀 보존(전이 유도) 뉘앙스 유지.
- **예산↔별점**: InkController.chargePlaced(cap) → InkBudget.totalUsed(초기−잔량) → computeStars.inkUsed.
- **틱 순서 고정**: LevelSession.tick = game.tick(방출→CA→전이) → flasks.update. 결정적.

### [4] 통합 결정성 — PASS (독립 재현, 이벤트 로그 포함)
LevelSession.tick을 GameState+FlaskSystem으로 재현(flutter 미의존)해 WATER 방출+서리선+고체 플라스크
창발 씬 600틱:
- **그리드 해시 ^ 착수이벤트로그 해시**(flaskIndex·x·y·물질·상 순서 인코딩) 3회 동일 = 0xa2c4dca4.
  → 착수 이벤트 **순서**까지 결정적(리더 지시 반영).
- reset 3회 재현 그리드 해시 동일 = 0xba675bc1. GDD 10.5 충족.
- 리더 지시 "결정성 해시에 이벤트 로그 포함" 달성. FlaskSystem 스캔 순서(fi→y→x) 고정이 이벤트 순서
  결정성의 근거.

### [5] 코어 루프 완주 — PASS (GDD 14장 첫 항목)
독립 구동: PRIMA 방출→플라스크 착수→**클리어#1(true)** → reset → **미해결(playing) 복귀** →
재구동 → **재클리어#2(true)**, 별점 통합(0잉크≤floor(20×1.15)=23 → ★3). 시작→플레이→클리어→재시작→
결과→재플레이 사이클이 로직 레벨에서 완주. 위젯 레벨은 widget_test("M2 게임 화면 빌드+리셋") +
LevelPlayer(_outcome 배너·_retry) + level_session_test가 커버. **GDD 14장 "코어 루프 완주 가능" 충족.**

### [6] 성능 회귀 — PASS (dart JIT, 51200셀·300틱, 예산 3ms)
| 시나리오 | M2 실측 | M1 baseline | 판정 |
|---|---|---|---|
| [A] 체커보드 WATER | 0.748 ms/tick | 0.942 | OK |
| [B] 체커보드 STEAM | 0.647 ms/tick | 0.732 | OK |
| [C] 화염룬+대량 WATER | 1.090 ms/tick | 1.354 | OK |
| [통합세션] 방출+CA+전이+**플라스크 스캔** | 0.261 ms/tick (활성 7723) | — | OK |
M1 대비 회귀 없음(JIT 변동 내 동등~소폭 낮음). **플라스크 판정 패스 오버헤드는 무시할 수준**
(통합 세션 틱 0.261ms = 예산 8.7%). 전부 예산 3ms 이내.

### [7] 별점 통합 — PASS
InkBudget.totalUsed = totalInitial − totalRemaining("사용량"). LevelSession.result가 이를
computeStars(inkUsed)에 전달, optimalTotal/explicit로 임계 결정. level_session_test "0잉크→3성" 통과.
정수 퍼센트(115/160) 부동소수 함정 회피 유지.

### [8] 레벨 JSON 왕복 무손실 — PASS
width가 loader(파싱, 기본 1)·serializer(기록)·validator(width≥1, 밴드 그리드내)에 완전 처리.
serializer_roundtrip_test가 emitter.width도 assert(통합 시 갱신 확인). 픽스처 width:13(밴드 74..86,
그리드 내). 로더↔직렬화↔모델 3자 정합 + EditorDocument 왕복 무손실.

### [9] 매직 넘버 — PASS
emitter.dart/level_session.dart에 밸런스 리터럴 없음(0.0 ashRatio 기본값, rate<1 방어 1은 알고리즘 가드).
별점 배율은 명명 상수, 레벨 밸런스는 JSON. 방출 기본값은 SimConstants.

## 권고 (비차단)
1. level_player.dart는 디버그 플레이 UI로 색·치수 하드코딩(0xFFC9A227 등). ui↔tokens 규칙(lib/ui 대상)
   위반 아니고 M4+ 셸이 대체 예정(debug_hud와 동일 취급) — 대체 시 잔존 방지 메모.
2. (1차 이월) validator chapter(1~4)·difficulty(1~10) 인라인 범위를 named const로 뽑으면 스키마 동기화
   명확(선택). 밸런스값 아님.

## 보류 (기준 미충족 아님, 환경 제약)
- Android 실기기 60fps 프로파일: 툴체인 부재로 계측 불가. 순수 Dart JIT 벤치(예산 대비 큰 여유)로 대체.
  M0부터 이월된 동일 보류 항목. 툴체인 확보 후 과제.

## 회귀 확인 (이전 리포트 대비)
- 1차(qa_m2_gameplay) 항목 전부 유지 + 통합 완결. width 왕복 커버리지 갭 우려는 해소(테스트가 assert).
- M1 성능 baseline 대비 회귀 없음. M1 상전이 결정성 원리(RNG 규율) 통합 씬에서도 유지.
- sim API frozen 유지(sim-engineer): 상전이·이동 코어 무변경, game_state는 방출구 다중화만 확장.

## 산출물
- 리포트: _workspace/qa_m2_gate_20260716.md (+ 1차 _workspace/qa_m2_gameplay_20260716.md)
- 독립 검증: LevelSession 재현 스크립트(임시, 삭제) — 이벤트로그 결정성 + 코어루프 완주 + 성능.
